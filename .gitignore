__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.DS_Store

# Generated route artifacts — regenerate with `python main.py`. The committed
# reference renders live in `samples/` instead.
output/

# Pytest / playwright scratch
.pytest_cache/
prototype/.pytest_cache/
.playwright-mcp/

# strav.art source-image cache — fair-use scratch, not redistributed.
# Re-fetch with `python tools/stravart_extract/scrape.py`. The extracted
# normalized templates live under `templates_stravart/` and are tracked.
scratch/
