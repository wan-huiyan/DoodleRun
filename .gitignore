__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.DS_Store

# Generated route artifacts — regenerate with `python main.py`. The committed
# reference renders live in `samples/` instead.
output/

# Pytest / playwright scratch
.pytest_cache/
prototype/.pytest_cache/
.playwright-mcp/

# OSMnx graph cache — multi-MB GraphML files; regenerate from network as needed
prototype/graph_cache/

# OSMnx HTTP response cache (Overpass JSON dumps) — hundreds of MB.
prototype/cache/

# Quick Draw! ndjson dumps — multi-MB raw datasets; regenerate via
# `python tools/quickdraw_to_shape.py`. The curated outlines committed
# under prototype/quickdraw_variants/ are the consumed artifact.
prototype/quickdraw_data/
