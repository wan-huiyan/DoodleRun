__pycache__/
*.pyc
*.pyo
.venv/
venv/
.venv-*/
.env
.DS_Store

# Generated route artifacts — regenerate with `python main.py`. The committed
# reference renders live in `samples/` instead.
output/

# Pytest / playwright scratch
.pytest_cache/
prototype/.pytest_cache/
.playwright-mcp/

# OSMnx HTTP cache (created at runtime by stravart/mapmatch.py).
# Per-bbox JSON; regenerated automatically on cache miss.
/cache/
