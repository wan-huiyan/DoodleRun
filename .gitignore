__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.DS_Store

# Generated route artifacts — regenerate with `python main.py`. The committed
# reference renders live in `samples/` instead.
output/

# Pytest / playwright scratch
.pytest_cache/
prototype/.pytest_cache/
.playwright-mcp/

# DoodleRun v2 (Quick Draw + strav.art template pipeline)
# Raw upstream data is gitignored; only extracted normalized templates are committed.
quickdraw_v2/data/*.recognized.ndjson
quickdraw_v2/data/strav_raw/
quickdraw_v2/refs/
# Scratch diagnostic renders — regenerate with debug_*.py if needed.
quickdraw_v2/diagnostics/
