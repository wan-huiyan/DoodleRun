"""Phase 4a proof-of-concept driver. Not part of the shipped pipeline.

This is a standalone batch runner that exercises the full Phase 3
reconstruction pipeline (image -> GPX) on a hand-curated 20-image
sample, captures full per-image diagnostics, and emits 4-panel
side-by-side PNGs for human review.
"""
